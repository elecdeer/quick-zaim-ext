# タスク一覧

完了したタスクは `[ ]` を `[x]` に変更する。

## Ph1

### 支払い登録フォーム

- [x] App.tsx を画面ごとに分離する（メイン画面 / 未認証状態の案内）
- [x] 金額・日付・コメントの入力フィールドを実装する
- [x] カテゴリ（大カテゴリ → サブカテゴリ連動）の combobox を実装する
- [x] 口座の combobox を実装する
- [x] 店舗の combobox を実装する（過去履歴から候補表示 + フリー入力）
- [x] 登録ボタン押下で `POST /api/zaim/payment` を呼び出す
- [x] 登録成功・失敗のフィードバックを表示する
- [x] 送信前に `GET /api/zaim/payment/duplicate` で重複チェックを行う
- [x] 重複検出時にフォーム内インライン警告を表示する（登録ボタンは警告色で押せる）

### LLM 自動入力

- [x] content script を追加して現在のページの内容（ARIA 属性・テキスト）を収集する
- [x] サーバーに `POST /api/llm/extract-payment` エンドポイントを追加する（Anthropic API キーは Workers 環境変数で管理）
- [x] LLM プロンプトを設計する（ページ内容 + カテゴリ/店舗/口座一覧 → 支払い情報 JSON）
- [x] 「このページから自動入力」ボタンを実装する（ローディング表示あり）
- [x] LLM レスポンスでフォームに値をセットする

### 設定オーバーレイ

- [x] メイン画面に設定ボタンを追加する
- [x] 設定オーバーレイ（モーダル）コンポーネントを作成する
- [x] 設定オーバーレイ内にサーバー URL 入力・保存を移動する
- [x] 設定オーバーレイ内にサーバー認証（ログイン/ログアウト）を移動する
- [x] 設定オーバーレイ内に Zaim 連携（接続/解除）を移動する

### フォローアップ

- [x] DateをTemporal APIに置き換える
- [x] コンポーネントは常にkumo-uiのものを使用する
- [x] LLMレイテンシ改善
  - input tokenを減らすプロンプト設計
- [x] カテゴリとサブカテゴリは1つのComboboxにする
  - カテゴリでgroupingして、サブカテゴリを選択する
- [x] ヘッダの「金額」だけ右寄せになっているのを直す
- [x] 横幅が狭いので、メモの値はPopoverでホバー時に表示する様にし、値の編集はモーダルで行う様にする
- [x] 品目名の編集もモーダルに移す
- [x] （サブ）カテゴリは品目ごとに選択できるようにする
- [x] 日付の入力は独自のカレンダーポップアップにする（直接数字で入力するのも維持する）
- [x] Comboboxのポップアップ開いた時、選択肢の所までスクロールする
- [x] 登録しました、はトーストで表示する

## Ph2

### ミスリカバリー

- [ ] サーバーに直近の登録アイテム取得エンドポイントを追加する（`GET /api/zaim/payment/recent`）
- [ ] サーバーに登録アイテム更新エンドポイントを追加する（`PUT /api/zaim/payment/:id`）
- [ ] メイン画面に「最近の登録」セクションを追加する（直近 N 件を一覧表示）
- [ ] 各アイテムの「編集」ボタンを押すとフォームにそのアイテムの値をセットする
- [ ] フォームが編集モードのとき、登録ボタン押下でアイテムを更新する（PUT）

### 最近の入力

- [ ] 最近選択したカテゴリ・サブカテゴリ・口座・店舗数件をcomboboxの選択肢上の方に表示する（localStorageに保存）

### LLM ヒントプロンプト設定

- [ ] 設定オーバーレイに「LLM ヒントプロンプト」テキストエリアを追加する
- [ ] 保存したヒントプロンプトを自動入力時に LLM プロンプトへ追加する
